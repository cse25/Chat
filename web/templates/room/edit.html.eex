<%= form_for @changeset, room_path(@conn, :update, @room), fn f -> %>
  <div class="form-group">
    <%= text_input f, :room, class: "form-control" %>
    <%= error_tag f, :room %>
  </div>
  <%= submit "Edit Room", class: "btn btn-primary" %>
<% end %>
